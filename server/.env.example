# Anthropic API (optional if TEST_MODE=true)
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-20250514

# Test Mode: Use mock responses instead of Claude API (useful for testing without API key)
TEST_MODE=false

# Optional: Set fixed password (otherwise random each time)
# REMOTE_AI_PASSWORD=your-secure-password-here
STREAM_URL=
REMOTE_AI_PASSWORD=
